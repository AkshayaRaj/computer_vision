function [ output_image ] = extend_boundary( input_image,window )
% used to increase image size before convolution with the filter of size
% window x window

% we push the edge pixels to accomodate window size

[a,b]=size(input_image);



end

